package com.rz.core.shop.global.exception;

public class RestErrorHandler{

//extends ResponseEntityExceptionHandler {

	/*
	 * private static final Logger logger =
	 * LoggerFactory.getLogger(RestErrorHandler.class);
	 * 
	 * @ExceptionHandler(Exception.class) public ResponseEntity<ErrorMessage>
	 * globalExceptionHandler(Exception ex, WebRequest request) { ErrorMessage
	 * message = new ErrorMessage(new Date(),
	 * HttpStatus.INTERNAL_SERVER_ERROR.value(), ex.getMessage(), "Error:" +
	 * ex.getMessage(), request.getDescription(false));
	 * 
	 * return new ResponseEntity<ErrorMessage>(message,
	 * HttpStatus.INTERNAL_SERVER_ERROR); }
	 * 
	 * @ExceptionHandler(BadCredentialsException.class) public
	 * ResponseEntity<ErrorMessage> BadCredentialsException(Exception ex, WebRequest
	 * request) { ErrorMessage message = new ErrorMessage(new Date(),
	 * HttpStatus.UNAUTHORIZED.value(), ex.getMessage(), "Error:" + ex.getMessage(),
	 * request.getDescription(false));
	 * logger.error("Error in authentication :"+ex.getMessage()); return new
	 * ResponseEntity<ErrorMessage>(message, HttpStatus.UNAUTHORIZED); }
	 */
}
